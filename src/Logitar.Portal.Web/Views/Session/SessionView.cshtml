@using Logitar.Portal.Core.Sessions.Models;

@model SessionModel
@{
  Layout = "_Layout";

  ViewBag.Title = "View session";
}

<session-view json="@(ModelSerializer.Serialize(Model))"></session-view>
