@using Logitar.Portal.Contracts.Messages;

@model MessageModel
@{
  Layout = "_Layout";

  ViewBag.Title = "View message";
}

  <message-view json="@(ModelSerializer.Serialize(Model))"></message-view>
